<UserControl x:Class="YetAnotherProfitCalc.WPF.UI.EveItemDropDown"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300" Height="27"
             xmlns:local="clr-namespace:YetAnotherProfitCalc.WPF.UI">
    <ComboBox IsEditable="True" StaysOpenOnEdit="True" IsTextSearchEnabled="False"
              ItemsSource="{Binding Path=Items,Mode=TwoWay}"
              Text="{Binding Path=Input,Mode=TwoWay}"
              DisplayMemberPath="ItemName"
              IsDropDownOpen="{Binding Path=DropdownShouldBeOpen}"
              Name="ComboBox">
    </ComboBox>
</UserControl>
